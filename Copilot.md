# Copilot Instructions

## Command Rules

1. When the user issues the `/triage` command, load and follow the skill defined in `skills/issue_triage/SKILL.md`.
2. When the user issues the `/debug` command, load and follow the skill defined in `skills/issue_debug/SKILL.md`.
