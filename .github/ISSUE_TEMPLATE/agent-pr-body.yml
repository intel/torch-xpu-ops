# Agent PR body template
# Used by render_pr_body() to format pull requests created by the agent pipeline.
# Placeholders use Python str.format() syntax: {field_name}

name: "Agent PR Body"
description: "Template for PRs submitted by the pytorch-agent pipeline"

body: |
  ## Summary

  Fix for [{upstream_issue_repo}#{source_number}]({issue_url})

  **Issue:** {title}

  {root_cause_section}
  {failed_tests_section}
  {failure_type_section}
  {diff_stat_section}
  {reviewer_section}
