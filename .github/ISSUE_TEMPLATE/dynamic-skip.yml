name: 🐛 Dynamic skip
description: Create an issue to skip PR unrelated failures dynamically
title: "[Bug Skip]: "
labels: ["skipped"]

# This form is the one definition of a skip issue, for a human and for the
# nightly bot alike. ut_create_issues.py reads it and renders one `### <label>`
# section per textarea below, in this order, filling each by its `id`. Add,
# reorder or rename a field and the bot follows; only a new `id` needs the
# script to learn what goes in it.
#
# It also reads the `labels:` above, and the three below that no form can
# express, because a form cannot set a label from what a field contains:
# the first follows the machine that ran the job, the others follow this run's
# comparison against the last healthy nightly. A `persistent` or `unknown`
# failure deliberately has no classification label.
#
# bot-label bmg-runner: skipped_bmg
# bot-label cls-regression: regression
# bot-label cls-new_case_failure: new_case_failure
#
# The `Cases:` block is what mutes, and it is parsed by fetch_issues.sh, by the
# awk filter in _linux_ut.yml and by mark_passed_issue in ut_result_check.sh.
# Between them the contract is: a line starting with `Cases:`, one case per
# line with no blank lines inside, and a blank line to end it. Nothing else in
# a body may contain the string `Cases:`, or the awk filter starts collecting
# again there.

body:
- type: markdown
  attributes:
    value: >
      #### Before submitting a bug, please make sure the issue hasn't been already addressed by searching through [the existing and past issues](https://github.com/pytorch/pytorch/issues?q=is%3Aissue+sort%3Acreated-desc+).
- type: textarea
  id: cases
  attributes:
    label: 🐛 Describe the bug with skip template
    description: |
      Please provide a clear and concise description of what the bug is.
      The template for dynamic skip as below:
      
      ```python
      # Template(Check in the github action summary)
      Cases:
      [Category],[Class name],[Test name]
      ```

      ```python
      # example
      Cases:
      op_ut,third_party.torch-xpu-ops.test.xpu.test_transformers_xpu.TestTransformersXPU,test_scaled
      ```

      If the code is too long (hopefully, it isn't), feel free to put it in a public gist and link it in the issue: https://gist.github.com.

      Please also paste or describe the results you observe instead of the expected results. If you observe an error, please paste the error message including the **full** traceback of the exception. It may be relevant to wrap error messages in ```` ```triple quotes blocks``` ````.
    placeholder: |
      A clear and concise description of what the bug is and also align the dynamic template.
      ```
      # Skippped cases with dynamic template
      ```

      ```python
      # Sample code to reproduce the problem
      ```

      ```
      The error message you got, with the full traceback.
      ```
  validations:
    required: true
- type: textarea
  id: summary
  attributes:
    label: Summary
    description: One to three sentences on what is failing and why these cases are one bug.
  validations:
    required: false
- type: textarea
  id: error_log
  attributes:
    label: ErrorLog
    description: The failure message, and the traceback if you have it.
  validations:
    required: false
- type: textarea
  id: reproduce
  attributes:
    label: Reproduce
    description: The directory to run from and the pytest invocation.
  validations:
    required: false
- type: textarea
  id: pytorch_version
  attributes:
    label: Pytorch Version
    description: |
      Where the failure started, if you know: the last run that passed these
      cases and the first that did not, with their torch and torch-xpu-ops
      commits.
  validations:
    required: false
- type: textarea
  id: versions
  attributes:
    label: Versions
    description: |
      Please run the following and paste the output below.
      ```sh
      wget https://raw.githubusercontent.com/pytorch/pytorch/main/torch/utils/collect_env.py
      # For security purposes, please check the contents of collect_env.py before running it.
      python collect_env.py
      ```
  validations:
    required: true
- type: markdown
  attributes:
    value: >
      Thanks for contributing 🎉!
