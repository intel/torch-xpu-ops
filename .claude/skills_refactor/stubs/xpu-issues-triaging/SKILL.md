---
name: xpu-issues-triaging
description: >
  Moved to fix/triage. Use that skill instead.
---

# Moved to fix/triage

This skill has been refactored into `fix/triage`.

Use `fix/triage` directly. It contains the same root cause analysis, target
repo decision, and IMPLEMENTING/NEEDS_HUMAN verdict logic — with pipeline mode
issue body writes removed (those now belong to the orchestrator).
